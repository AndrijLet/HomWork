"""Створити вищу функцію, яка буде застосовувати операцію перетворення"""
# def transform_list(data, operation):
#     return [operation(item) for item in data]
#
# odd_or_even = lambda x: "even" if x % 2 == 0 else "odd"
#
# list = [1, 2, 3, 4, 5, 6, 7, 8, 9, 13, 16, 25]
#
#
# result = transform_list(list, odd_or_even)
# print(result)

"""Задача 3 — перетворити всі слова у списку на ВЕЛИКІ ЛІТЕРИ.
Task 3 — convert all words in the list to UPPERCASE."""
# def transform_list(data, operation):
#     return [operation(item) for item in data]
#
# words = ["apple", "banana", "cherry"]
#
# to_upper = lambda w: w.upper()
# result = transform_list(words, str.upper)
# print(result)

"""Task - 4"""